# 残作業

2026-09-23 時点。番号は会話で指示するときの識別子として使う（例:「TODO.md の 6 番から進めて」）。
完了したら行を消すのではなく ~~取り消し線~~ にして、番号を振り直さないこと。

## A. 仕様の未確定点（実機・エミュレータでの確認が必要）

根拠と資料間の差異は `docs/research.md` の該当節を参照。

1. **能力値の端数処理**（§7.2）: ⌊基準値×0.9⌋（組長式）か四捨五入（GCGX 計算機）か。現状は切り捨て、UI では「参考値」表示。
   実装: `src/core/growth.ts` の `adjust()`。
2. **基準値 0（Lv1-2 の MP）に A を加えるか**（§7.2）: 現状は「加えない」（GCGX の記述を採用）。実装は同じく `adjust()`。
3. **名前入力画面の実際の文字配置**（§4.2）: 現状は五十音順の参考配置。実装: `src/core/tables.ts` の `NAME_PALETTE_ROWS`。
4. **名前入力の制約**（§4.2）: 名前の途中に空白を置けるか、任意の文字の後に ゛゜ を付けられるか。
   例: 「ほりいゆうじ…」の名前「おっ゜て」（っ の後の ゜）が名前入力で作れるか。判明したら `NameChar.inputability` に反映する。
5. **decode 時の副作用**（§3 末尾）: pattern bit2 が RAM `$E4` bit7 に入る件は文書化のみで未モデル化。
   ゲーム上の意味が分かれば導出情報として表示を検討。

## B. 未実装・改善候補

6. ~~**120bit Overview の凡例 hover**~~ → 完了（凡例は `cat:<カテゴリ>` の仮想ノード。`relate()` が所属フィールドの関連を合成する。2026-09-23）
7. ~~**UI の E2E テスト**~~ → 完了（`e2e/app.spec.ts`。`npm run test:e2e`、CI でも公開前に実行。2026-09-23）
8. **キーボード操作**: Tab で巡回できるのはフィールド・byte・6bit 値・文字の約 100 個。bit セルは `tabbable={false}`。
   列内を矢印キーで移動する roving focus などを検討。
9. **タッチ端末の実機確認**: タップで選択が固定されるつくりだが、実機では未確認。
10. ~~**スマホ表示**~~ → 完了（700px 以下ではインスペクタ本文を 2 行に畳み、「全文」で展開・「固定中 ×」で解除。2026-09-23）
11. **導出ステータスの配線**: 名前・EXP → レベル・成長タイプはハイライトのみで、SVG の線は無い（パイプラインの外にあるため）。
15. ~~**Explorer の URL 化（hash ルーティング）**~~ → 完了（`#/?p=<呪文>` / `#/check?p=<呪文>`。`src/ui/route.ts`。2026-09-23）

## C. 品質

12. **オラクル比較の偏り**: 「チェックは合うが実機が拒否する」decode ベクターが 1 件（道具 ID 15）のみ。
    やくそう・かぎの上限超えは自作テストだけでカバー。`scripts/gen-oracle-vectors.mjs` で、自作 encoder で作った
    該当呪文をオラクルに decode させるベクターを追加する。
13. **性能**: hover ごとに約 700 ノードを再描画している。低スペック端末で計測し、必要ならクラス付けを DOM 直接操作に切り替える。
14. ~~**ドキュメント**~~ → 対応不要として完了（上流の間接影響を 1 ホップに留める理由は `relate()`、変更経路を両端が変わった辺に限る理由は
    `diffSnapshots()` のコメントに記載済み。2026-09-23）

## 推奨順

~~6 → 7 → 10 → 15~~ 完了。次は 9（タッチ実機確認。公開 URL で確認できる）→ 8（キーボード）
